<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Interest Calculator</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <%- include('header') %>

    <div class="container mt-4">
        <h2>Interest Calculator</h2>
        <div class="card col-md-8 offset-md-2 mt-4">
            <div class="card-body">
                <form id="calculatorForm">
                    <div class="mb-3">
                        <label for="calcAmount" class="form-label">Principal Amount</label>
                        <input type="number" class="form-control" id="calcAmount" required>
                    </div>

                    <div class="mb-3">
                        <label for="calcRate" class="form-label">Interest Rate (% per year)</label>
                        <input type="number" step="0.01" class="form-control" id="calcRate" required>
                    </div>

                    <div class="mb-3">
                        <label for="calcTime" class="form-label">Time Period (Years)</label>
                        <input type="number" step="0.01" class="form-control" id="calcTime" required>
                    </div>

                    <div class="mb-3">
                        <label for="calcType" class="form-label">Calculation Type</label>
                        <select class="form-select" id="calcType">
                            <option value="simple">Simple Interest</option>
                            <option value="compound">Compound Interest (Annually)</option>
                        </select>
                    </div>

                    <button type="button" class="btn btn-primary" onclick="calculateInterest()">Calculate</button>
                    <button type="reset" class="btn btn-secondary" onclick="document.getElementById('calcResult').style.display = 'none';">Reset</button>
                </form>

                <div id="calcResult" class="mt-4 alert alert-info" style="display: none;">
                    <h5>Results</h5>
                    <p>Total Interest: <span id="resInterest">0</span></p>
                    <p>Total Amount (Principal + Interest): <span id="resTotal">0</span></p>
                </div>
            </div>
        </div>
    </div>

    <script>
        function calculateInterest() {
            const p = parseFloat(document.getElementById('calcAmount').value);
            const r = parseFloat(document.getElementById('calcRate').value);
            const t = parseFloat(document.getElementById('calcTime').value);
            const type = document.getElementById('calcType').value;

            if (isNaN(p) || isNaN(r) || isNaN(t)) {
                alert("Please enter valid numbers");
                return;
            }

            let interest = 0;
            let total = 0;

            if (type === 'simple') {
                interest = (p * r * t) / 100;
                total = p + interest;
            } else if (type === 'compound') {
                total = p * Math.pow((1 + r / 100), t);
                interest = total - p;
            }

            document.getElementById('resInterest').innerText = interest.toFixed(2);
            document.getElementById('resTotal').innerText = total.toFixed(2);
            document.getElementById('calcResult').style.display = 'block';
        }
    </script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>